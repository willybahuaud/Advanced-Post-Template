{
  "name": "advanced-post-template",
  "version": "0.1.0",
  "private": true,
  "description": "Gutenberg Query Loop Post Template variation with slice controls.",
  "scripts": {
    "start": "wp-scripts start src/index.js --output-path=build",
    "build": "wp-scripts build src/index.js --output-path=build",
    "lint:js": "wp-scripts lint-js",
    "format": "wp-scripts format"
  },
  "devDependencies": {
    "@wordpress/scripts": "^27.1.0"
  }
}
